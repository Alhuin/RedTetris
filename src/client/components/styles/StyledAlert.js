import styled from 'styled-components';

export const StyledAlertWrapper = styled.div`
   width: 100vw;
   position: absolute;
   top: 0;

   svg {
     cursor: pointer;
     font-size: inherit;
   }
`;
